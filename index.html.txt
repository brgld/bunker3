<!doctype html>
<html lang="hr">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width,initial-scale=1" />
  <title>Bunker Night — Invite</title>
  <style>
    :root{
      --shadow: 0 20px 60px rgba(0,0,0,.55);
      --tape: rgba(240,240,240,.8);
    }

    /* Brick wall-ish background without needing an image */
    body{
      margin:0;
      min-height:100vh;
      display:grid;
      place-items:center;
      background:
        linear-gradient(90deg, rgba(0,0,0,.35), rgba(0,0,0,.35)),
        repeating-linear-gradient(0deg, #6b4a3a 0 18px, #5e4134 18px 20px),
        repeating-linear-gradient(90deg, rgba(255,255,255,.05) 0 80px, rgba(0,0,0,.08) 80px 82px);
      font-family: system-ui, -apple-system, Segoe UI, Roboto, Arial;
      padding: 24px;
    }

    .scene{
      width:min(980px, 100%);
      display:grid;
      grid-template-columns: 1fr;
      gap:16px;
      justify-items:center;
    }

    .poster-wrap{
      position:relative;
      width:min(760px, 100%);
      aspect-ratio: 3 / 4; /* adjust if needed */
      display:block;
      filter: drop-shadow(var(--shadow));
      transform: rotate(-1.2deg);
    }

    .poster{
      width:100%;
      height:100%;
      object-fit:cover;
      border-radius: 10px;
      background:#000;
    }

    /* “Tape” corners */
    .tape{
      position:absolute;
      width: 110px; height: 42px;
      background: var(--tape);
      box-shadow: 0 6px 12px rgba(0,0,0,.25);
      border-radius: 6px;
      opacity:.9;
    }
    .t1{ top:-16px; left:22px; transform: rotate(-10deg); }
    .t2{ top:-18px; right:26px; transform: rotate(12deg); }
    .t3{ bottom:-18px; left:36px; transform: rotate(8deg); }
    .t4{ bottom:-16px; right:30px; transform: rotate(-12deg); }

    .actions{
      width:min(760px,100%);
      display:flex;
      gap:12px;
      flex-wrap:wrap;
      justify-content:center;
      align-items:center;
    }

    /* Spray paint vibe */
    .spray-btn{
      position:relative;
      border:0;
      cursor:pointer;
      padding: 16px 18px;
      border-radius: 14px;
      background: rgba(0,0,0,.35);
      color:#fff;
      font-weight:800;
      letter-spacing:.2px;
      font-size: 18px;
      text-transform: uppercase;
      box-shadow: 0 14px 28px rgba(0,0,0,.35);
      backdrop-filter: blur(6px);
      overflow:hidden;
    }
    .spray-btn::before{
      content:"";
      position:absolute;
      inset:-40%;
      background:
        radial-gradient(circle at 20% 30%, rgba(255,0,80,.55), transparent 45%),
        radial-gradient(circle at 70% 40%, rgba(255,200,0,.45), transparent 50%),
        radial-gradient(circle at 50% 70%, rgba(255,255,255,.10), transparent 55%);
      transform: rotate(8deg);
      opacity:.9;
      pointer-events:none;
    }
    .spray-btn span{ position:relative; }

    .small-link{
      color: rgba(255,255,255,.75);
      text-decoration: underline;
      font-size: 14px;
      cursor:pointer;
      background: transparent;
      border:0;
      padding: 10px 8px;
    }

    .toast{
      width:min(760px,100%);
      display:none;
      padding: 12px 14px;
      border-radius: 12px;
      background: rgba(0,0,0,.45);
      color:#fff;
      box-shadow: 0 10px 26px rgba(0,0,0,.35);
    }
    .toast strong{ color: #ffe28a; }
  </style>
</head>

<body>
  <main class="scene">
    <div class="poster-wrap" aria-label="Bunker Night poster on wall">
      <img class="poster" src="bunker-night-3.png" alt="Bunker Night invite poster" />
      <div class="tape t1"></div><div class="tape t2"></div><div class="tape t3"></div><div class="tape t4"></div>
    </div>

    <div class="actions">
      <button class="spray-btn" id="yesBtn">
        <span>NARAVNO DA DOLAZIM, NISAM LOSER</span>
      </button>
      <button class="small-link" id="noBtn">Ne mogu / javi mi detalje kasnije</button>
    </div>

    <div class="toast" id="toast"></div>
  </main>

  <script>
    const toast = document.getElementById("toast");

    function showToast(html){
      toast.innerHTML = html;
      toast.style.display = "block";
      toast.scrollIntoView({behavior:"smooth", block:"nearest"});
    }

    function setRsvp(value){
      const url = new URL(window.location.href);
      url.searchParams.set("rsvp", value);
      window.history.replaceState({}, "", url);
    }

    document.getElementById("yesBtn").addEventListener("click", () => {
      setRsvp("yes");
      showToast(`Top. <strong>Vidimo se.</strong> 🥂<br/>Pošalji samo 👍 u chat i to je to.`);
    });

    document.getElementById("noBtn").addEventListener("click", () => {
      setRsvp("no");
      showToast(`Ok, fer. 😄<br/>Pošalji “ne mrem” i nitko ti ništa neće (osim malo suda).`);
    });
  </script>
</body>
</html>